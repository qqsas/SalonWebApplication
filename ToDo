add "payment gateway for demonstration"
set up all messageing services for each action
    Done with the vendor php plugin added to the project (needs to be online to test)
add feature tab for admin dashboard (do at end as not all features are ready)
    Turn payment gateway on and off
    Allow barbers to edit/add new services/products
Remove name from barber sql or automate the link between user name and barber name
Add display/sort options for orders.php
